# Create fusion app
CLI tool to bootstrap your new Fusion application

To run:
* `pnpm install`
* `pnpm build`
* `pnpm test:js`

Usage:  `npm create fusion-app@latest`

Including:
* Ag Grid
* EDS
* Fusion Context
* React Query
* Routing
